import { useState } from 'react';
import { Alert, Button, Group, Paper, Select, SimpleGrid, Stack, Text } from '@mantine/core';
import { ConfirmModal } from './ConfirmModal';
const API='http://localhost:3001/api'; type Asset={id:string;url:string;version:number;active:boolean};type Artwork={id:string;platform:string;sourceImage?:{id:string;url:string}|null;variants:Asset[]};
const labels:Record<string,string>={youtube:'YouTube','youtube-shorts':'YouTube Shorts',tiktok:'TikTok',spotify:'Spotify',landscape:'Generic Landscape',vertical:'Generic Vertical',square:'Generic Square'};
async function api(path:string,method:string,body?:unknown){const r=await fetch(API+path,{method,headers:{'Content-Type':'application/json'},body:body===undefined?undefined:JSON.stringify(body)});if(!r.ok)throw new Error((await r.json()).error||'Could not update artwork.');return r.json();}
export function ArtworkPage({project,artwork,shots,onRefresh}:{project:any;artwork:Artwork[];shots:any[];onRefresh:()=>Promise<void>}){const[platform,setPlatform]=useState(project.primary_visual_format||'landscape'),[count,setCount]=useState(3),[strategy,setStrategy]=useState('Cinematic'),[busy,setBusy]=useState(false),[error,setError]=useState(''),[confirm,setConfirm]=useState<'generate'|'delete'|null>(null),[asset,setAsset]=useState<string|null>(null);const targets:string[]=JSON.parse(project.publishing_targets||'[]');const active=artwork.find(a=>a.platform===platform);const shotsWithAssets=shots.map(s=>({...s,asset:s.generations?.find((g:any)=>g.active&&g.assetId)||s.generations?.find((g:any)=>g.assetId)})).filter(s=>s.asset);const run=async(f:()=>Promise<unknown>)=>{setBusy(true);setError('');try{await f();await onRefresh()}catch(e){setError(e instanceof Error?e.message:'Could not complete artwork action.')}finally{setBusy(false)}};return <main className="artwork-page"><header><div><h1>Artwork</h1><p>Create platform-ready thumbnails and covers without changing your storyboard.</p></div></header><Paper className="panel" p="lg"><Stack><h2>Thumbnail generation</h2><SimpleGrid cols={{base:1,sm:3}}><Select label="Target" value={platform} onChange={v=>setPlatform(v||'landscape')} data={(targets.length?targets:['youtube','tiktok','spotify','landscape','vertical','square']).map(id=>({value:id,label:labels[id]}))}/><Select label="Creative direction" value={strategy} onChange={v=>setStrategy(v||'Cinematic')} data={['Cinematic','Character focused','Dramatic','Minimal','Mysterious','High contrast','Custom']}/><Select label="Variants" value={String(count)} onChange={v=>setCount(Number(v||3))} data={['2','3','4']}/></SimpleGrid><Button loading={busy} onClick={()=>setConfirm('generate')}>Generate {count} variants</Button>{error&&<Alert color="red">{error}</Alert>}</Stack></Paper><Paper className="panel" p="lg"><h2>{labels[platform]} artwork</h2><Text className="safe-area">Safe-area guides are preview-only and never baked into downloads.</Text><SimpleGrid className="comparison-grid" cols={{base:1,sm:2,lg:3}}>{active?.sourceImage&&<Paper className="comparison-image" p="sm"><img src={active.sourceImage.url} alt="Selected storyboard source"/><Text size="sm">Selected storyboard image · linked, not copied</Text></Paper>}{active?.variants.map(v=><Paper className="comparison-image" p="sm" key={v.id}><img src={v.url} alt={`Artwork version ${v.version}`}/><Text size="sm">Version {v.version}{v.active?' · ACTIVE':''}</Text><Group><Button component="a" variant="default" href={`${API}/assets/${v.id}/download`}>Download</Button><Button color="red" disabled={busy} onClick={()=>{setAsset(v.id);setConfirm('delete')}}>Delete</Button></Group></Paper>)}</SimpleGrid>{!active?.sourceImage&&!active?.variants.length&&<Text>No artwork yet.</Text>}</Paper><Paper className="panel" p="lg"><h2>Use storyboard image</h2><Text>Selecting a shot links it here. It does not create or copy an image.</Text><SimpleGrid className="version-thumbnails" cols={{base:2,sm:4,lg:6}}>{shotsWithAssets.map(s=><Button key={s.id} variant="default" disabled={busy} onClick={()=>void run(()=>api(`/projects/${project.id}/artwork/from-source`,'POST',{platform,assetId:s.asset.assetId}))}><img src={s.asset.assetUrl} alt=""/>Use shot {s.order}</Button>)}</SimpleGrid></Paper><ConfirmModal opened={confirm==='generate'} title="Generate artwork variants?" message={`Generate ${count} ${labels[platform]} variants? This is a paid generation action.`} confirmLabel={`Generate ${count} variants`} loading={busy} onCancel={()=>setConfirm(null)} onConfirm={()=>void run(()=>api(`/projects/${project.id}/artwork/generate`,'POST',{platform,count,strategy,focus:'Automatic',text:'',subtitle:''})).finally(()=>setConfirm(null))}/><ConfirmModal opened={confirm==='delete'} title="Delete artwork version?" message="Delete this artwork version? This cannot be undone." confirmLabel="Delete" confirmColor="red" loading={busy} onCancel={()=>setConfirm(null)} onConfirm={()=>{if(asset&&active)void run(()=>api(`/projects/${project.id}/artwork/${active.id}/assets/${asset}`,'DELETE')).finally(()=>setConfirm(null));}}/></main>}
